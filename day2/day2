1. 웹 브라우저(Chrome) - HTML문서를 해석하여 사용자인터페이스 제공

2. 태그(Tag), 요소(Element), 속성(Atttribute)

태그 : 속성을 갖는 객체

3. 요소 : Title, Body, 이미지, 버튼, 링크, 주소...

4. 요소를 만들 때 사용하는 작성 방법 : 태그

5. 자바스크립트에서는 요소를 객체(Object)라고도 한다.

6. 웹페이지는 두 가지 방법 CSS 적용
    1. 내부 스타일시트 적용 <style></style>
    2. 외부 스타일시트 적용 <link>

7. 웹페이지는 두 가지 방법 자바스크립트 적용
    1. 내부 자바스크립트 적용 <script></script>
    2. 외부 자바스크립트 적용 외부에 js 파일 -> 적용할 문서에 script src 속성을 이용하여 적용한다.

8. 오류와 검증
    프로그램이 원하지 않는 방향으로 동작(버그) -> 버그를 잡는다.(수정) = 디버그

9. HTML 기본 태그
    - 글자 태그
        * 앵커 <a></a>
        * 제목, 본문
        * 글자 모양

    - 목록 태그
        * 순서 있는 리스트
        * 순서 없는 리스트
        * 중첩 리스트

    - 테이블 태그
        * 테이블과 셀 생성
        * 행 병합, 열 병합

    - 미디어 태그 (오디오, 비디오, 이미지)
        * 앵커
        * 글자 모양

10. 미디어 태그
이미지, 오디오, 비디오 (멀티미디어)를 HTML문서에 삽입하는 태그
<img> -> src (이미지 경로 지정), alt(이미지 없을 때 나오는 글자 설정), width(이미지의 너비), height(이미지의 높이)
<audio></audio> -> src(음악,동영상 파일 경로 지정), preload, autoplay, loop, controls
<video></video> -> width, height

1. 요소
2. 태그
3. 시작, 끝
4. 속성
5. 주석
6. 스타일
7. 링크
8. 스크립트
9. JS